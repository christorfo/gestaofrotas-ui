import { FormatCpfPipe } from './format-cpf-pipe';

describe('FormatCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
